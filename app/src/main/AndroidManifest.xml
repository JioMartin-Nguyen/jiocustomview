<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jio.customview.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="jio.customview.app.common.imageloader.MyCustomGlide"
            android:value="AppGlideModule"
            ></meta-data>
        <activity android:name=".presentation.activity.CacheImageActivity2"></activity>
        <activity android:name=".presentation.activity.PlayVideoActivity" />
        <activity android:name=".presentation.activity.CacheImageActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".presentation.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.activity.JioRecycleViewActivity" />
        <activity android:name=".presentation.activity.BaseActivity" />
    </application>

</manifest>